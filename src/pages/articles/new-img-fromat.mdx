import Layout from '~/components/layout'

export const meta = {
  title: '【WebP, JPEG-XR】新画像フォーマットでの画像配信',
  date: '2021/06/15',
  description: '新画像フォーマット、WebP JPEG-XR で画像配信をする。',
  tags: ['Frontend', 'Google'],
}

# test h1

export default ({ children }) => (
  <Layout meta={meta} type="post">
    {children}
  </Layout>
)
